// 探索シーン（町・フィールド歩き回り）— 今後実装予定
class ExploreScene extends Phaser.Scene {
  constructor() { super('Explore'); }
  create() {
    this.add.text(20, 20, '探索シーン（実装予定）', { fontSize: '16px', color: '#ffffff' });
  }
}
